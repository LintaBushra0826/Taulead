import React from 'react';
import CreateprocessIcon from '@mui/icons-material/Create';

const createIcon = () => {
  return (
    <div className="toolbar-icon">
      <CreateprocessIcon/>
     </div>
  );
};

export default createIcon;