import React from "react";
import { AiOutlineMenu } from "react-icons/ai";

function MenuIcon() {
  return (
      <AiOutlineMenu style={{  
        position: 'fixed',
        bottom: '88%',
        right: '83%',
        color:'white',
        borderRadius:'2px',
        borderColor:'white', }}/>
  );
}

export default MenuIcon;
